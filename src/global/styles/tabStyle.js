import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
    container:{
        width: '100%',
        height: '8%',
        backgroundColor: '#EBEBEB',
        borderRadius: 20,
        marginTop:5
    }
})